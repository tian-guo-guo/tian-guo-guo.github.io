I"�<h2 id="linux--git-command-not-found-错误解决">Linux – git: command not found 错误解决</h2>

<h2 id="出错原因">出错原因</h2>

<p>服务器没有安装GIT，所以导致出错。</p>

<h2 id="解决方法">解决方法</h2>

<p><strong>Centos下使用：</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>yum install git -y
</code></pre></div></div>

<p>或者</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>yum install -y git
</code></pre></div></div>

<p>两个代码都是一样的，随意的使用一个即可。</p>

<p><strong>Ubuntu/Debian下使用</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt-get install [git](https://www.bxl.me/b/git/) -y
</code></pre></div></div>

<p>即可解决问题。</p>
:ET